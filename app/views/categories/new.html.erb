<div class="container">
  <div class="row py-3 m-lg-0 mx-sm-5 mx-2">
    <div class="col-lg">
      <div class="p-3 mx-2 mb-3 mb-lg-0 border rounded bg-displaydish">
        <div class="text-center text-danger fw-bold pb-2">
          <div class="d-md-block d-none">
            <i class="fa-solid fa-triangle-exclamation"></i>
            注意点
          </div>
          <button type="button" class="btn border-none d-md-none" data-bs-toggle="offcanvas" data-bs-target="#offcanvasBottom">
            <i class="fa-solid fa-triangle-exclamation text-danger"></i>
            <span class="text-danger fw-bold">注意点</span>
          </button>
        </div>
        <%= render "layouts/shared/error_messages", model: @category %>
        <div class="d-md-block d-none">
          <ul class="mt-3 list-unstyled">
            <li class="p-3 mb-3 border rounded">カテゴリ名は、必ず入力する</li>
            <li class="p-3 border rounded">作成したら、カテゴリ一覧に追加されているか確認する</li>
          </ul>
        </div>
        <div class="offcanvas offcanvas-bottom bg-displaydish" data-bs-scroll="true" data-bs-backdrop="false" tabindex="-1" id="offcanvasBottom">
          <div class="offcanvas-header">
            <div></div>
            <button type="button" class="btn-close" data-bs-dismiss="offcanvas"></button>
          </div>
          <div class="offcanvas-body">
            <ul class="mt-3 list-unstyled">
              <li class="mb-3 fw-bold text-danger">カテゴリ名は、必ず入力する</li>
              <li class="fw-bold text-danger">作成したら、カテゴリ一覧に追加されているか確認する</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
    <div class="col-lg">
      <div class="p-3 mx-2 mb-3 mb-lg-0 border rounded bg-displaydish" style="max-height: 330px;">
        <h4 class="text-center">カテゴリ作成</h4>
        <%= render "layouts/shared/form", model: @category %>
      </div>
    </div>
    <div class="col-lg">
      <div class="p-3 mx-2 border rounded bg-displaydish overflow-auto" style="max-height: 330px;">
        <h4>現在のカテゴリ一覧</h4>
        <table class="table table-warning table-striped">
          <tr>
            <th class="text-center" scope="col">id</th>
            <th scope="col">カテゴリ名</th>
          </tr>
          <% @categories.each do |category| %>
            <tr>
              <th class="text-center" scope="row"><%= category.id %></th>
              <td><%= category.name %></td>
            </tr>
          <% end %>
        </table>
      </div>
    </div>
  </div>
</div>
